{
  "name": "ricki-rubin-real-estate-website",
  "version": "1.0.0",
  "private": true,
  "description": "Ricki Rubin real estate website with server-side Bridge/Stellar MLS search.",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "check": "node --check app.js && node --check server.mjs && node --check script.js && node --check search.js"
  },
  "engines": {
    "node": ">=22"
  }
}
